apiVersion: apps/v1
kind: Deployment
metadata:
    name: agent-two-depl
spec:
    replicas: 1
    selector:
        matchLabels:
            app: agent-two
    template:
        metadata:
            labels:
                app: agent-two
        spec:
            containers:
                - name: agent-two
                  image: kneonix/agent:latest
                  imagePullPolicy: Never
                  env:
                      - name: KAFKA_BROKER_URI
                        valueFrom:
                            secretKeyRef:
                                name: shared-secret
                                key: KAFKA_BROKER_URI
                      - name: PORT
                        value: "3000"
                      - name: AGENT_ID
                        value: "2"
                      - name: KAFKA_TOPIC
                        value: "agent-events"
